<?php
$host = "localhost";
$username = "root";
$password = "";
$database_name = "imageupload";

$conn = mysqli_connect($host, $username, $password, $database_name);
/* if ($conn) {
    echo "database connection successful";
}else{
    echo "could not connect too the database";
} */